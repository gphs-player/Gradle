#
# 配置文件，部分属性${}动态配置
#
hostname=${databaseHostname}
appVersion=${version}
locale=en_us
initialConnections=10
transferThrottle=5400
queueTimeout=30000
buildNumber=${buildNumber}
buildDate=${date.format("yyyy-MM-dd' 'HH:mm:ssZ")}